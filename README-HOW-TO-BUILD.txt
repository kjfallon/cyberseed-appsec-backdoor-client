
BUILDING:

This project may be built with maven3.  You need maven on your path as well as Java 8 JDK.

We included a build.sh that will invoke maven for you or you may do it yourself.

The jar will be built in the ./target directory


$ mvn -DskipTests clean package

OR

$ ./build.sh
